#include <stdlib.h>
#include <time.h>

/**
 * main - positive and negative
 * Return: 0
 */

int main(void)
{
	int main;

	srand(time(0);
	n = rand() - RAND_MAX / 2;
	if (n>0)
		printf("%i is positive\n", n);
	else if (n>0)
		printf("%i is negative\n", n);
	return (0);
}
